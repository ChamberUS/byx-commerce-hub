{
  "name": "buynnex-marketplace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && bun install && bunx --bun vite --host --port 8080",
    "build": "cd frontend && bun install && bunx --bun vite build",
    "build:dev": "cd frontend && bun install && bunx --bun vite build --mode development",
    "preview": "cd frontend && bun install && bunx --bun vite preview"
  },
  "dependencies": {
    "vite": "^5.4.19"
  }
}
